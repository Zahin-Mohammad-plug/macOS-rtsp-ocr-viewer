# Copy this file to `.env` for local QA only. Do not commit real stream URLs.

# Primary private QA stream used for local smoke tests.
SHARPSTREAM_TEST_RTSP_URL=rtsp://YOUR_PRIVATE_HOST:554/live

# Optional local file path used for playback/seek/smart-pause smoke tests.
# SHARPSTREAM_TEST_VIDEO_FILE=/absolute/path/to/test.mp4

# Optional comma-separated fallback stream list.
# SHARPSTREAM_TEST_STREAMS=rtsp://host1:554/live,https://example.com/stream.m3u8
