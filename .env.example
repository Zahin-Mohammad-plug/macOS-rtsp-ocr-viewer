# Copy to .env and fill in your values. .env is gitignored.
# Used by development docs and test scripts (e.g. Smart Pause validation).

# Path to a local test video file (e.g. test.MOV in project root)
SHARPSTREAM_TEST_VIDEO_FILE=

# Optional: RTSP URL for live-stream tests
SHARPSTREAM_TEST_RTSP_URL=

# Optional: Custom path for debug log (default: ~/Library/Application Support/SharpStream/debug.log)
# SHARPSTREAM_DEBUG_LOG_PATH=
